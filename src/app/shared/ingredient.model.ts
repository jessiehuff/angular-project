export class Ingredient {
    constructor(public name: string, public amount: number){} 
    //will automatically give us properties with names we specify as argument names and assign the values to these properties

}